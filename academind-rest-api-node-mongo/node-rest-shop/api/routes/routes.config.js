const baseApiRoute = "/api";

const productRoute = `${baseApiRoute}/products`;
const ordersRoute = `${baseApiRoute}/orders`;
const usersRoute = `${baseApiRoute}/users`;

module.exports = {
  productRoute,
  ordersRoute,
  usersRoute,
};
